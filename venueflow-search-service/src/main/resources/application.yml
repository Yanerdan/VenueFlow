spring:
  application:
    name: venueflow-search-service
  profiles:
    default: skeleton
  cloud:
    sentinel:
      enabled: false
    discovery:
      enabled: false
    service-registry:
      auto-registration:
        enabled: false
    nacos:
      discovery:
        enabled: false
      config:
        enabled: false

server:
  port: ${SERVER_PORT:8086}

management:
  opentelemetry:
    enabled: false
  endpoints:
    access:
      default: none
    web:
      exposure:
        include: health
      discovery:
        enabled: false
  endpoint:
    health:
      access: read-only
      show-details: never
      show-components: never
      probes:
        enabled: true
  health:
    elasticsearch:
      enabled: false
    rabbit:
      enabled: false
