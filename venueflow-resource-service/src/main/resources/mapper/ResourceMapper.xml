<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yanerdan.venueflow.resource.catalog.persistence.mapper.ResourceMapper">

  <resultMap
    id="ResourceEntityResultMap"
    type="com.yanerdan.venueflow.resource.catalog.persistence.entity.ResourceEntity">
    <id column="id" property="id"/>
    <result column="resource_no" property="resourceNo"/>
    <result column="category_id" property="categoryId"/>
    <result column="name" property="name"/>
    <result column="description" property="description"/>
    <result column="location" property="location"/>
    <result column="capacity" property="capacity"/>
    <result column="owner_department" property="ownerDepartment"/>
    <result column="approver_external_user_id" property="approverExternalUserId"/>
    <result column="status" property="status"/>
    <result column="version" property="version"/>
    <result column="created_at" property="createdAt"/>
    <result column="updated_at" property="updatedAt"/>
  </resultMap>

  <sql id="ResourceColumns">
    id,
    resource_no,
    category_id,
    name,
    description,
    location,
    capacity,
    owner_department,
    approver_external_user_id,
    status,
    version,
    created_at,
    updated_at
  </sql>

  <select id="countResources" resultType="long">
    SELECT COUNT(*)
    FROM `resource`
    <where>
      <if test="categoryId != null">
        category_id = #{categoryId}
      </if>
      <if test="status != null">
        AND status = #{status}
      </if>
    </where>
  </select>

  <select
    id="selectResourcePage"
    resultMap="ResourceEntityResultMap">
    SELECT
    <include refid="ResourceColumns"/>
    FROM `resource`
    <where>
      <if test="categoryId != null">
        category_id = #{categoryId}
      </if>
      <if test="status != null">
        AND status = #{status}
      </if>
    </where>
    ORDER BY id ASC
    LIMIT #{limit}
    OFFSET #{offset}
  </select>

  <update id="updateStatusIfVersionMatches">
    UPDATE `resource`
    SET status = #{targetStatus},
    version = version + 1,
    updated_at = CURRENT_TIMESTAMP(6)
    WHERE id = #{resourceId}
    AND status = #{currentStatus}
    AND version = #{expectedVersion}
  </update>

  <update id="updateOwnershipIfVersionMatches">
    UPDATE `resource`
    SET owner_department = #{ownerDepartment},
    approver_external_user_id = #{approverExternalUserId},
    version = version + 1,
    updated_at = CURRENT_TIMESTAMP(6)
    WHERE id = #{resourceId}
    AND version = #{expectedVersion}
  </update>

  <select id="selectByIdForUpdate" resultMap="ResourceEntityResultMap">
    SELECT <include refid="ResourceColumns"/>
    FROM `resource`
    WHERE id = #{resourceId}
    FOR UPDATE
  </select>

</mapper>
