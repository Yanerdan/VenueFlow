<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="              http://maven.apache.org/POM/4.0.0              https://maven.apache.org/xsd/maven-4.0.0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.yanerdan.venueflow</groupId>
    <artifactId>venueflow-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <relativePath>../pom.xml</relativePath>
  </parent>

  <artifactId>venueflow-notification-service</artifactId>
  <packaging>jar</packaging>

  <name>VenueFlow Notification Service</name>

  <description>Infrastructure-independent Notification Service skeleton.</description>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>com.yanerdan.venueflow</groupId>
        <artifactId>venueflow-dependencies</artifactId>
        <version>${project.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-webmvc</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>
    <dependency>
      <groupId>io.micrometer</groupId>
      <artifactId>micrometer-registry-prometheus</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-opentelemetry</artifactId>
    </dependency>
    <dependency>
      <groupId>com.alibaba.cloud</groupId>
      <artifactId>spring-cloud-starter-alibaba-nacos-discovery</artifactId>
    </dependency>
    <dependency>
      <groupId>com.alibaba.cloud</groupId>
      <artifactId>spring-cloud-starter-alibaba-nacos-config</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-jdbc</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-flyway</artifactId>
    </dependency>

    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-mysql</artifactId>
    </dependency>

    <dependency>
      <groupId>com.mysql</groupId>
      <artifactId>mysql-connector-j</artifactId>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-amqp</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>testcontainers-junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>testcontainers-mysql</artifactId>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>testcontainers-rabbitmq</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>

        <configuration>
          <mainClass>com.yanerdan.venueflow.notification.NotificationServiceApplication</mainClass>
        </configuration>

        <executions>
          <execution>
            <goals>
              <goal>repackage</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>com.github.spotbugs</groupId>
        <artifactId>spotbugs-maven-plugin</artifactId>

        <configuration>
          <excludeFilterFile>${project.basedir}/spotbugs-exclude.xml</excludeFilterFile>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>

        <executions>
          <execution>
            <id>enforce-notification-boundary</id>

            <goals>
              <goal>enforce</goal>
            </goals>

            <configuration>
              <rules>
                <bannedDependencies>
                  <message>Notification Service may directly
                    depend only on its approved Web,
                    Actuator, validation, JDBC, Flyway,
                    MySQL, AMQP and test support.</message>

                  <searchTransitive>false</searchTransitive>

                  <excludes>
                    <exclude>*:*</exclude>
                  </excludes>

                  <includes>
                    <include>org.springframework.boot:spring-boot-starter-webmvc</include>
                    <include>org.springframework.boot:spring-boot-starter-actuator</include>
                    <include>io.micrometer:micrometer-registry-prometheus</include>
                    <include>org.springframework.boot:spring-boot-starter-opentelemetry</include>
                    <include>com.alibaba.cloud:spring-cloud-starter-alibaba-nacos-discovery</include>
                    <include>com.alibaba.cloud:spring-cloud-starter-alibaba-nacos-config</include>
                    <include>org.springframework.boot:spring-boot-starter-validation</include>
                    <include>org.springframework.boot:spring-boot-starter-jdbc</include>
                    <include>org.springframework.boot:spring-boot-starter-flyway</include>
                    <include>org.flywaydb:flyway-mysql</include>
                    <include>com.mysql:mysql-connector-j</include>
                    <include>org.springframework.boot:spring-boot-starter-amqp</include>
                    <include>org.springframework.boot:spring-boot-starter-test</include>
                    <include>org.testcontainers:testcontainers-junit-jupiter</include>
                    <include>org.testcontainers:testcontainers-mysql</include>
                    <include>org.testcontainers:testcontainers-rabbitmq</include>
                  </includes>
                </bannedDependencies>
              </rules>
            </configuration>
          </execution>
          <execution>
            <id>enforce-notification-forbidden-transitives</id>

            <goals>
              <goal>enforce</goal>
            </goals>

            <configuration>
              <rules>
                <bannedDependencies>
                  <message>Notification Service must remain
                    free of other services, mail, security,
                    discovery, cache, search and tracing.</message>

                  <searchTransitive>true</searchTransitive>

                  <excludes>
                    <exclude>com.yanerdan.venueflow:venueflow-*</exclude>
                    <exclude>org.springframework.boot:spring-boot-starter-mail</exclude>
                    <exclude>jakarta.mail:*</exclude>
                    <exclude>org.springframework.data:spring-data-redis</exclude>
                    <exclude>io.lettuce:*</exclude>
                    <exclude>redis.clients:*</exclude>
                    <exclude>org.springframework.kafka:*</exclude>
                    <exclude>org.apache.kafka:*</exclude>
                    <exclude>org.elasticsearch:*</exclude>
                  </excludes>
                </bannedDependencies>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-failsafe-plugin</artifactId>

        <executions>
          <execution>
            <goals>
              <goal>integration-test</goal>
              <goal>verify</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <profile>
      <id>consumer-it</id>

      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-failsafe-plugin</artifactId>

            <executions>
              <execution>
                <id>run-notification-consumer-integration-tests</id>

                <goals>
                  <goal>integration-test</goal>
                  <goal>verify</goal>
                </goals>

                <configuration>
                  <includes>
                    <include>**/*ConsumerSuite.java</include>
                  </includes>

                  <failIfNoTests>true</failIfNoTests>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>

</project>
