## 1. Version and Environment Baseline

- [x] 1.1 Upgrade the engineering specification to version 1.1.0 and align Elasticsearch with the Spring Data 2025.1 baseline.
- [x] 1.2 Replace the fixed VMware topology with parameterized addresses and executable 16GB runtime modes.

## 2. Consistency and Idempotency Rules

- [x] 2.1 Add scoped idempotency keys, request fingerprints, operation IDs, and an auditable slot allocation model.
- [x] 2.2 Correct the pending status name and clarify that confirmation is not real third-party payment.

## 3. Bootstrap Governance

- [x] 3.1 Align the v0.1.0 milestone with the three ordered bootstrap changes.
- [x] 3.2 Add the one-time pre-bootstrap exception and Windows `openspec.cmd` execution note.

## 4. Verification

- [x] 4.1 Validate the OpenSpec change and run textual consistency checks against the revised engineering specification.
