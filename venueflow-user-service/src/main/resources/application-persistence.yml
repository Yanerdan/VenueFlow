spring:
  config:
    activate:
      on-profile: persistence

  datasource:
    url: ${VENUEFLOW_USER_DB_URL}
    username: ${VENUEFLOW_USER_DB_USERNAME}
    password: ${VENUEFLOW_USER_DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  flyway:
    enabled: true
    locations: classpath:db/migration
    validate-on-migrate: true
    clean-disabled: true
    baseline-on-migrate: false

  sql:
    init:
      mode: never

  jpa:
    hibernate:
      ddl-auto: none

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
